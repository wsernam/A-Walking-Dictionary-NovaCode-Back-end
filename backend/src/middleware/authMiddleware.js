// authMiddleware.js
// APLAZADO: la autenticación/autorización se implementará al final del desarrollo,
// una vez que las funcionalidades core (mazos, tarjetas, aportes, quiz) funcionen
// sin la capa de login de por medio.
// Pendiente (cuando se retome): verificar token JWT en el header Authorization,
// validar rol (docente/estudiante/invitado) contra el endpoint solicitado.
// Depende de: AuthService.js, variable de entorno JWT_SECRET.
